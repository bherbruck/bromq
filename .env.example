# MQTT Server Configuration (optional - defaults are set in code)

# Database
MQTT_DB_PATH=/app/data/mqtt-server.db

# Server Ports
MQTT_TCP_PORT=:1883
MQTT_WS_PORT=:8883
HTTP_PORT=:8080

# JWT Secret (⚠️ CHANGE THIS IN PRODUCTION!)
# JWT_SECRET=your-secret-key-here

# TLS Configuration (future use)
# MQTT_TLS_ENABLED=false
# MQTT_TLS_CERT=/path/to/cert.pem
# MQTT_TLS_KEY=/path/to/key.pem
