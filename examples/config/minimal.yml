# yaml-language-server: $schema=https://github.com/bromq-dev/bromq/releases/latest/download/bromq-config.schema.json
# Minimal BroMQ Configuration Example
# For testing and simple deployments

users:
  - username: test_device
    password: ${TEST_PASSWORD}
    description: "Test device"

acl_rules:
  - username: test_device
    topic: "test/${username}/#"
    permission: pubsub

# Optional: Bridge to remote broker
# bridges:
#   - name: simple-bridge
#     host: mqtt.example.com
#     port: 1883
#     username: bridge_user
#     password: ${BRIDGE_PASSWORD}
#     topics:
#       - local: "data/#"
#         remote: "remote/data/#"
#         direction: out
#         qos: 0
