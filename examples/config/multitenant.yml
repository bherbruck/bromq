# Multi-tenant MQTT Configuration Example
# For SaaS IoT platforms with isolated tenant namespaces

users:
  # Tenant A devices
  - username: tenant_a_devices
    password: ${TENANT_A_PASSWORD}
    description: "Tenant A IoT devices"
    metadata:
      tenant_id: "tenant-a"
      plan: "premium"
      max_devices: 100

  # Tenant B devices
  - username: tenant_b_devices
    password: ${TENANT_B_PASSWORD}
    description: "Tenant B IoT devices"
    metadata:
      tenant_id: "tenant-b"
      plan: "basic"
      max_devices: 10

  # Tenant C devices
  - username: tenant_c_devices
    password: ${TENANT_C_PASSWORD}
    description: "Tenant C IoT devices"
    metadata:
      tenant_id: "tenant-c"
      plan: "enterprise"
      max_devices: 1000

acl_rules:
  # Tenant A - isolated namespace with ${username} and ${clientid}
  - username: tenant_a_devices
    topic: "tenants/tenant-a/${clientid}/#"
    permission: pubsub

  # Tenant B - isolated namespace
  - username: tenant_b_devices
    topic: "tenants/tenant-b/${clientid}/#"
    permission: pubsub

  # Tenant C - isolated namespace
  - username: tenant_c_devices
    topic: "tenants/tenant-c/${clientid}/#"
    permission: pubsub

# Optional: Bridge tenant data to central cloud platform
# bridges:
#   # Forward Tenant A data to their dedicated cloud instance
#   - name: tenant-a-cloud
#     host: ${TENANT_A_CLOUD_HOST}
#     port: 1883
#     username: ${TENANT_A_CLOUD_USER}
#     password: ${TENANT_A_CLOUD_PASSWORD}
#     metadata:
#       tenant_id: "tenant-a"
#     topics:
#       - local: "tenants/tenant-a/#"
#         remote: "cloud/customer-a/#"
#         direction: out
#         qos: 1
#
#   # Forward Tenant B data to shared cloud
#   - name: tenant-b-cloud
#     host: shared-cloud.example.com
#     port: 1883
#     username: shared_bridge
#     password: ${SHARED_CLOUD_PASSWORD}
#     metadata:
#       tenant_id: "tenant-b"
#     topics:
#       - local: "tenants/tenant-b/#"
#         remote: "shared/tenant-b/#"
#         direction: out
#         qos: 0
